{
  "comment": [
    "Ground truth for the target-app fixture. This file lives OUTSIDE fixtures/target-app on",
    "purpose: reset-sandbox.sh copies only the app into .sandbox/, so the agent under test",
    "never sees the answer key while deriving suites from the code.",
    "verify-fixture.mjs drives every probe below against the live stack and fails the run when",
    "the fixture and this key disagree — without that guard, a drifted fixture would quietly",
    "turn recall into a meaningless number.",
    "asserts/run.js derives its recall denominator from the `ac` fields here, so a defect added",
    "below is scored without touching the assertion.",
    "Every probe must be non-mutating: this runs before the eval, and a probe that changed the",
    "store would both move the app out from under the run and break its store-restored invariant.",
    "KNOWN AMBIGUITY, deliberately unscored: PATCH accepts `{\"status\":\"rejected\"}`, which the",
    "Domain's two-value lifecycle makes unsupported (422) but which no AC names. Reporting it is",
    "legitimate and reporting it is optional, so it counts as neither a miss nor a false positive."
  ],
  "personas": {
    "admin": { "email": "admin@example.test", "password": "eval-admin-pw" },
    "member": { "email": "member@example.test", "password": "eval-member-pw" }
  },
  "defects": [
    {
      "id": "D1",
      "surface": "api",
      "ac": "AC-4",
      "summary": "GET /api/admin/audit returns 200 for a member; the guard tests role truthiness instead of equality to admin",
      "probe": { "persona": "member", "method": "GET", "path": "/api/admin/audit" },
      "specified": 403,
      "actual": 200,
      "keywords": ["audit", "admin"]
    },
    {
      "id": "D2",
      "surface": "fault",
      "ac": "AC-5",
      "summary": "GET /api/projects fails open when the PDP is unreachable, returning 200 with cross-org projects instead of 503",
      "probe": { "persona": "member", "method": "GET", "path": "/api/projects", "pdpPaused": true },
      "specified": 503,
      "actual": 200,
      "actualBodyContains": "p-2",
      "keywords": ["projects", "fail", "open", "503"]
    },
    {
      "id": "D3",
      "surface": "ui",
      "ac": "AC-7",
      "summary": "the nav-admin entry renders on /dashboard for a member; the template omits the role check",
      "probe": { "persona": "member", "method": "GET", "path": "/dashboard", "ui": true },
      "specifiedAbsent": "data-testid=\"nav-admin\"",
      "actualPresent": "data-testid=\"nav-admin\"",
      "keywords": ["nav", "admin", "member"]
    },
    {
      "id": "D4",
      "surface": "api",
      "ac": "AC-3",
      "summary": "PATCH /api/memberships/{id} exempts admins from the ownership check, so an admin may patch another user's membership where the spec denies it unconditionally",
      "probe": {
        "persona": "admin",
        "method": "PATCH",
        "path": "/api/memberships/m-1",
        "body": { "status": "active" }
      },
      "specified": 403,
      "actual": 409,
      "keywords": ["membership", "ownership", "admin", "403"],
      "note": [
        "m-1 belongs to the member and is already active, so the ownership carve-out lets this",
        "call through to the not-pending check and it answers 409 instead of 403. Getting past",
        "403 at all is the defect; landing on 409 is what keeps the probe from mutating the store."
      ]
    }
  ],
  "correct": [
    {
      "ac": "AC-1",
      "note": "admin sees both projects",
      "probe": { "persona": "admin", "method": "GET", "path": "/api/projects" },
      "expect": 200,
      "expectBodyContains": ["p-1", "p-2"]
    },
    {
      "ac": "AC-1",
      "note": "member sees only the org they hold an active membership in",
      "probe": { "persona": "member", "method": "GET", "path": "/api/projects" },
      "expect": 200,
      "expectBodyContains": ["p-1"],
      "expectBodyExcludes": ["p-2"]
    },
    {
      "ac": "AC-2",
      "note": "member cannot delete a project",
      "probe": { "persona": "member", "method": "DELETE", "path": "/api/projects/p-1" },
      "expect": 403
    },
    {
      "ac": "AC-3",
      "note": "a project in an org the member only holds a pending membership in is denied",
      "probe": { "persona": "member", "method": "GET", "path": "/api/projects/p-2" },
      "expect": 403
    },
    {
      "ac": "AC-4",
      "note": "admin may read the audit log",
      "probe": { "persona": "admin", "method": "GET", "path": "/api/admin/audit" },
      "expect": 200
    },
    {
      "ac": "AC-5",
      "note": "single-project read is correctly fail-closed",
      "probe": { "persona": "member", "method": "GET", "path": "/api/projects/p-1", "pdpPaused": true },
      "expect": 503
    },
    {
      "ac": "AC-5",
      "note": "delete is correctly fail-closed",
      "probe": { "persona": "member", "method": "DELETE", "path": "/api/projects/p-1", "pdpPaused": true },
      "expect": 503
    },
    {
      "ac": "AC-6",
      "note": "no session is rejected before any authorization decision",
      "probe": { "persona": null, "method": "GET", "path": "/api/projects" },
      "expect": 401
    },
    {
      "ac": "AC-6",
      "note": "no session is still 401 while the PDP is down",
      "probe": { "persona": null, "method": "GET", "path": "/api/projects", "pdpPaused": true },
      "expect": 401
    },
    {
      "ac": "AC-7",
      "note": "admin does see the admin nav entry",
      "probe": { "persona": "admin", "method": "GET", "path": "/dashboard", "ui": true },
      "expectPresent": "data-testid=\"nav-admin\""
    },
    {
      "ac": "AC-8",
      "note": "a rejected sign-in shows the error marker",
      "probe": { "badLogin": true },
      "expectPresent": "data-testid=\"login-error\""
    }
  ]
}
