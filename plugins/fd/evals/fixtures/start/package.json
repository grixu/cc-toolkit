{
  "name": "acme-api",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny HTTP service used as the /fd:start fixture project.",
  "packageManager": "pnpm@9.12.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "format": "prettier --write ."
  }
}
