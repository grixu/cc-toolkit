---
description: "Mass-implement a directory of split tasks: dependency-ordered waves on a dynamic workflow, batched validation, then a push/PR proposal."
allowed-tools: Skill
argument-hint: "<path to the tasks directory>"
---

Invoke the `fd3:implement-tasks` skill with `$ARGUMENTS`, forwarded verbatim, and follow it to the end.
