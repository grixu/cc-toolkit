# scratch

An intentionally empty project: no notes file, no research directory, no design documents. The
write-missing-input-stop scenario invokes `fd3:write-spec` here with input paths that do not
resolve, and asserts that nothing is written.
