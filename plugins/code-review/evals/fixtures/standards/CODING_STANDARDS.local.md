# Local overrides

## Naming

- Exported functions need not declare an explicit return type; an inferred return type is fine in this checkout.

## Defaults

- A default value MAY be supplied with `??` rather than `||`, so that `0` and `''` survive.
