# Coding standards

## Naming

- Boolean-returning functions MUST be named with an `is`, `has`, or `can` prefix.
- Exported functions SHOULD declare an explicit return type.

## General

- Write clean code.
