# Node dev tooling (shared at the repo root; used by plugins/*/evals)
node_modules/
pnpm-lock.yaml
# pnpm-workspace.yaml is committed — it carries the onlyBuiltDependencies setting

# promptfoo eval outputs
eval-*.json
*.eval.json
output.json
.promptfoo/

# fd eval sandboxes — throwaway per-run copies of plugins/fd/evals/fixtures/ that the
# fd commands mutate; the pristine fixtures under fixtures/ stay committed.
plugins/fd/evals/.sandbox/

.DS_Store
